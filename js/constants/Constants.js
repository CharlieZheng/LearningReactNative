let avatarSize = 50
let colors ={
 purple : '#3B3571'
}
let sizes={
    paddingHorizontal:10,
    paddingVertical:10
}
let deviceHeight = require('Dimensions').get('window').height
let deviceWidth = require('Dimensions').get('window').width

module.exports = {avatarSize,colors,deviceHeight,deviceWidth ,sizes}